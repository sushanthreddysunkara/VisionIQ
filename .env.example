# Leave empty when using the Vite LAN proxy. Set a LAN URL only for a separately hosted frontend.
VITE_API_URL=
# Optional: enables the interactive Google Maps view on Vehicle Information.
VITE_GOOGLE_MAPS_API_KEY=
